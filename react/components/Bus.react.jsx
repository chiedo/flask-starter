var React = require('react');

var Bus = React.createClass({

  render: function() {
    return (
      <div className="bus">
        Bus with no wheels.
      </div>
    );
  }

});

module.exports = Bus;
